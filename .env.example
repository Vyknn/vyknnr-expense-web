# SQLite database location. Defaults to src/db/database.sqlite3 when omitted.
DATABASE_PATH=

# Required only once, when the database has no users. Store real values in Secret Manager.
INITIAL_ADMIN_EMAIL=
INITIAL_ADMIN_PASSWORD=
